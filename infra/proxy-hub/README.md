# Proxy Hub infrastructure

Proxy Hub deployment assets will live here once the minimum interface in `docs/proxy-hub.md` is implemented. Scholar database and corpus deployment assets must not be added to this directory.
