- 所有形式化实体（Innovation, Paper, Tool 等）均派生 `Repr` 和 `DecidableEq` 以支持运行时反射与自动化决策。
- 定理证明优先采用 `simp` 结合 `decide` 或 `omega` 的自动化策略，确保无 `sorry` 且证明过程简洁。
- 数据库定义采用扁平化的 `def` 列表形式，按研究领域或组件类型分组，便于 Python 脚本解析与同步。