- 所有技能文件均命名为 `SKILL.md` 并置于独立子目录下，遵循统一的元数据头（name/description）与分步执行流程结构。
- 规则文件采用 Markdown Frontmatter 格式（如 `alwaysApply: true`）来定义规则的适用范围与描述信息。
- 工作流触发机制在 `pipelines.md` 中通过关键词映射表进行集中配置，实现用户自然语言意图到具体 Skill 的路由。