- 所有命令实现均从 `_shared` 模块导入 `app`、`console` 和 `_get_db`，禁止在命令文件中重复定义 CLI 基础对象。
- 涉及外部服务（DB/API）的操作前必须检查可用性（如 `database.available`），失败时通过 `console.print` 给出警告而非直接抛出异常中断程序。
- 批量处理任务（如 `parse-all`）采用容错循环模式，单点失败记录在案并继续执行，最终在面板中统一汇总错误信息。