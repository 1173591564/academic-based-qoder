- 采用模块化 CLI 架构：`cli.py` 作为轻量入口，通过 `_shared.py` 共享 Typer 应用实例与数据库连接，并动态导入 `commands/` 下的功能子模块（如 `paper_ops`, `graph_ops`）以注册命令。
- 配置层 (`config.py`) 实现了开发模式与打包模式（PyInstaller）的双模适配，根据 `sys.frozen` 状态动态解析 `SCHOLAR_HOME` 全局知识库根目录。
- 依赖方向清晰：入口层依赖共享层，共享层依赖领域解析器与数据库模块，消除了循环导入。