- 所有暴露给 MCP 客户端的工具函数均使用 `@mcp.tool()` 装饰器标记，并配备详细的 docstring 以生成工具描述和参数说明。
- CLI 代理函数统一通过 `_run_scholar` 封装 `subprocess.run`，自动处理命令构建、超时控制（默认 120s，长任务可配置）、以及标准错误流的捕获与格式化。
- 涉及具体论文 ID 的文件读取操作（如 `read_parsed_paper`），均先调用 `scholar.id_resolver.resolve_id` 将多种格式的 ID（ULID/arXiv/DOI）标准化为 ULID，再构造文件路径。