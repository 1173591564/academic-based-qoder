- 采用多级降级策略（Multi-level Fallback）：在提取年份或元数据时，依次尝试精确匹配、模式识别、启发式推断及外部 API 检索，确保鲁棒性。
- 实施激进的 LaTeX 噪声清洗：在提取文本内容前，统一移除注释、格式化命令、数学环境及交叉引用标记，并保留核心语义文本。
- 遵循“仅填充缺失”原则：在元数据增强过程中，默认不覆盖已有字段，除非显式开启 `overwrite` 标志，以保护人工校对结果。
- 使用内存缓存与懒加载：ID 解析器在首次调用时扫描所有 JSON 构建索引，避免重复的文件 I/O 操作。