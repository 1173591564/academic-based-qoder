- 所有涉及列表或字典类型的字段（如 sections, authors, formulas）在解析前均使用 `ast.literal_eval` 进行安全转换，以兼容字符串格式的存储数据。
- 各功能模块均采用统一的“单点处理 + 批量遍历”双接口设计模式，批量函数内部调用单点逻辑并统一处理文件 I/O 与异常统计。
- 质量评分与分类逻辑中广泛使用正则表达式（`re.search` / `re.finditer`）进行语义特征匹配，并通过累加命中次数或特征权重来计算最终得分。