- 所有数据结构（如 Innovation, Paper, Properties）均显式派生 `Repr` 和 `DecidableEq` 以支持自动化推理与调试输出。
- 形式化定理证明统一采用 `simp` 结合 `decide` 的策略，利用编译期计算完成对自然数属性的逻辑验证。
- 数据库实例化遵循按研究领域（ResearchLine）分组的静态定义模式，每个创新节点均包含明确的量化属性（可扩展性、简洁性、稳定性）。