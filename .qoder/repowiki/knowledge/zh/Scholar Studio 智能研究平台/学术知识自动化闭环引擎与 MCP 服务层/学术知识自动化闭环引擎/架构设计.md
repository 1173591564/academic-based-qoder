- **共享上下文注入**：利用 `_shared.py` 集中管理 Typer 实例、控制台及数据库句柄，消除 CLI 层与业务模块间的循环依赖。
- **分层存储协同**：以 `kb_update.py` 为编排核心，同步协调 PostgreSQL（结构化）、Neo4j（图网络）与 Pgvector（语义索引）的数据一致性。
- **自适应研究闭环**：`research_loop.py` 基于日志分析动态提取兴趣，驱动定向搜索与全流程入库，形成“感知-决策-执行”的自动化管线。
- **弹性降级机制**：各存储模块均内置可用性探针，在外部服务缺失时自动降级为文件模式或跳过非核心步骤，确保核心解析功能可用。