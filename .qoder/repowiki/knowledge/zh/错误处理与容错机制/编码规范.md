- 采用“防御性编程”策略，在数据库初始化及 API 调用处广泛使用 try-except 捕获异常并返回 None 或布尔状态，确保主流程不因辅助服务故障而中断。
- 配置文件与关键状态文件（如 research-interests.json）的写入遵循“先写临时文件 .tmp，再 os.replace”的原子更新模式，防止进程崩溃导致数据损坏。
- CLI 错误处理统一使用 typer.Exit(1) 终止程序并配合 rich.Console 输出红色面板，保持用户反馈的一致性。
- 混合 ID 解析机制：在 MCP 工具及内部逻辑中，优先通过 `id_resolver` 将 arXiv ID/DOI 等映射为内部 ULID，确保跨模块引用的稳定性。