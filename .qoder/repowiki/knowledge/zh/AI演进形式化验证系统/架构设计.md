- 采用三层架构：`AiEvolution.Basic`定义核心类型（研究路线、属性指标、创新节点），`AiEvolution.Database`实例化125个创新节点与417篇论文的静态知识库，`AiEvolution.Theorems`基于前两层数据执行形式化定理证明。
- 入口点`Main.lean`作为可执行目标，负责初始化IO并输出验证状态，依赖根模块`AiEvolution.lean`聚合子模块。
- 构建系统由`lakefile.toml`驱动，声明了库目标`AiEvolution`和可执行目标`aievolution`，通过`lean-toolchain`锁定语言版本。