- 所有耗时操作均通过 `_run_scholar` 执行，并根据任务复杂度配置差异化的 `timeout` 参数（如解析类 120s，引导类 1200s）。
- 涉及论文 ID 的工具参数统一支持多种格式（ULID/arXiv/DOI/slug），并在文件读取类工具中强制调用 `resolve_id` 进行归一化。
- 具有副作用的维护类工具（如 `year-fix`, `author-fix`）默认采用干跑（dry-run）模式，需显式传入 `--apply` 或 `apply=True` 才执行写入。