- 所有模块均实现‘数据库优先，文件系统降级’策略，在PG/Neo4j不可用时自动回退至JSON文件操作。
- 外部API调用（arXiv, Embedding）统一实施3秒以上速率限制与指数退避重试机制。
- 关键状态文件（如兴趣画像、分析日志）写入时均采用‘先写.tmp再os.replace’的原子更新模式。
- 跨模块身份解析通过 `id_resolver.py` 单例缓存实现，支持ULID、arXiv ID、DOI及Slug的模糊归一化。