- **CLI框架**：`typer` 结合 `rich` 实现交互式命令行界面。
- **图数据库**：`neo4j` Python驱动用于构建引文网络与创新概念图谱。
- **向量检索**：`psycopg2` 配合 PostgreSQL `pgvector` 扩展及 HNSW 索引实现语义搜索。
- **嵌入服务**：支持智谱AI (`zhipu`) 及 OpenAI API 进行文本向量化。