- **图数据库**：使用 `neo4j` 驱动构建包含 Paper、Innovation 节点及 CITES/HAS_CONCEPT 关系的复杂图谱。
- **向量检索**：基于 `psycopg2` 操作 PostgreSQL `pgvector` 扩展，支持 HNSW 索引与余弦相似度搜索。
- **外部集成**：通过 `urllib` 直接调用 arXiv API (Atom XML) 和智谱/OpenAI Embedding API，无重型 SDK 依赖。