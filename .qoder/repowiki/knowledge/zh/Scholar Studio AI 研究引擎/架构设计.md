- 采用混合架构，将 Python 驱动的学术工作流（RAG、知识图谱）与 Lean4 形式化验证模块解耦，通过 CLI (`scholar`) 和 MCP 服务器进行交互。
- GitHub Actions 定义了跨语言验证流水线，在统一环境中协调 Python 依赖安装与 Lean4 `lake build` 的执行，确保逻辑一致性与代码可用性。
- `pyproject.toml` 作为核心配置枢纽，声明了 `scholar` 与 `scholar_mcp` 双包结构，并通过 optional-dependencies 管理开发工具链。