- 采用结构化记录（Structure）定义核心实体（如Innovation, Paper, Properties），并派生Repr与DecidableEq以支持自动化推理与打印。
- 在定理证明中广泛使用`simp`结合具体定义展开，随后通过`decide`策略完成基于自然数比较的自动化验证。
- 数据库模块采用扁平化的`def`定义列表存储大规模静态数据（如417篇论文），并通过命名前缀（如p_XXX）保持索引有序。