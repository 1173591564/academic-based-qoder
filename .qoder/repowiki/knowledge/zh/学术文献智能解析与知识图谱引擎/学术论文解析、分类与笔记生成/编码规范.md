- 采用多级降级策略提取关键字段（如年份提取依次尝试 ACM 宏、会议样式包、注释、arXiv ID 等十余种模式）。
- 在文本清洗管道中优先执行自定义宏（\newcommand/\def）解析，再进行 LaTeX 格式命令剥离。
- 使用深度嵌套的正则表达式以支持跨越多层花括号的 LaTeX 命令参数捕获。
- 对输入数据中的列表字段（如 authors, sections）同时兼容原生 list 类型与字符串化表示（通过 ast.literal_eval 转换）。