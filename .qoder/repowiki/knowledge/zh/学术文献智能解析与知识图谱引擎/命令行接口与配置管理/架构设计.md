- 采用 Typer 框架构建 CLI 应用，通过 `__main__.py` 作为模块执行入口调用 `cli.main()`。
- `config.py` 负责初始化全局路径常量（如 `PAPERS_DIR`, `OUTPUT_DIR`）并加载环境变量，支持 PostgreSQL、Neo4j 及 Embedding API 的配置读取。
- `cli.py` 定义了大量业务命令（如 `parse`, `graph-build`, `rag-index`），通过懒加载方式导入 `db`, `graph_db`, `rag` 等子模块以执行具体逻辑，实现了接口层与核心业务层的解耦。