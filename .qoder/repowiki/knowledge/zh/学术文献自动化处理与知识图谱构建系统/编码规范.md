- 所有模块均实现“数据库优先，文件回退”机制，当 PostgreSQL/Neo4j 不可用时自动降级为 JSON 文件操作以保证可用性。
- 在处理解析后的 JSON 数据时，普遍使用 `ast.literal_eval` 兼容字符串格式的列表/字典字段，增强对旧数据或不同解析源的容错性。
- 外部 API 调用（如 arXiv, Embedding）均内置重试机制与超时控制，并在 `config.py` 中统一封装请求逻辑以支持代理配置。