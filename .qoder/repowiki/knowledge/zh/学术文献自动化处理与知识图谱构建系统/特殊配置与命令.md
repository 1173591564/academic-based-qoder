- **环境变量**：需在 `.env` 中配置 `SCHOLAR_PG_*`, `SCHOLAR_NEO4J_*`, `SCHOLAR_EMBEDDING_API_KEY`。
- **启动命令**：`python -m scholar <command>` (如 `scan`, `parse-all`, `graph-build`, `rag-index`)。
- **一键更新**：执行 `kb-update` 可自动完成搜索、下载、解析、入库及笔记生成的完整闭环。