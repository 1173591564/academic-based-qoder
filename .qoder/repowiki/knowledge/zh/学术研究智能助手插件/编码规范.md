- 所有 Skills 均采用统一的 `SKILL.md` 结构，明确包含触发关键词、分步执行流程（Step-by-step）及后续建议（Next Steps）。
- 坚持数据驱动原则，要求所有学术声明必须通过 `python -m scholar search` 等工具获取 JSON 数据支撑，严禁编造论文信息。
- 输出产物统一存放于 `output/` 目录下的特定子文件夹（如 `output/drafts/`, `output/notes/`），便于追踪和复用。