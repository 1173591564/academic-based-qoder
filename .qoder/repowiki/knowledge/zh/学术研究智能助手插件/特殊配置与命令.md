- **后端依赖**：需克隆并初始化主仓库 `academic-based-qoder`，运行 `pip install -r requirements.txt` 及 `python -m scholar bootstrap`。
- **环境启动**：通过 `./startup.ps1` 启动 PostgreSQL (5433) 和 Neo4j (7687) Docker 容器。
- **验证命令**：在对话中输入 `调研 Transformer` 或 `知识库状态` 验证插件与后端的连通性。