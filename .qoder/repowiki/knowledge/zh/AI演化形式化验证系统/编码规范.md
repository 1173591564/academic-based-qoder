- 所有数据结构（如 `Innovation`, `Paper`）均派生 `Repr` 和 `DecidableEq` 类型类，以支持运行时打印和编译期决策过程。
- 形式化定理证明统一采用 `by` 块结合 `constructor`、`rfl` 和 `decide` 策略，确保所有关于创新替代关系的命题均通过机器验证且不含未完成的证明占位符。
- 数据库中的实体定义遵循统一的命名与实例化模式，每个创新节点均显式指定其所属的研究路线（`ResearchLine`）、年份及三维属性值。